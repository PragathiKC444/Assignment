# Visualizations

Add screenshots and plots here:
- Segmentation mask outputs
- Accuracy and loss curves
- Comparative bar charts

Example (Python):
```python
import matplotlib.pyplot as plt
plt.imshow(preds[0].squeeze(), cmap='gray')
plt.title('Predicted Road Segmentation Mask')
plt.show()
```
